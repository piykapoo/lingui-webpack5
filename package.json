{
  "name": "@sample/root",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "private": "true",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "yarn workspaces run build",
    "extract": "yarn workspaces run lingui extract",
    "compile": "yarn workspaces run lingui compile",
    "start": "yarn workspace @sample/app start"
  },
  "author": "",
  "license": "ISC",
  "resolutions": {
    "@lingui/react": "^3.17.2",
    "@lingui/core": "^3.17.2"
  },
  "devDependencies": {
    "ts-loader": "^9.4.4"
  }
}
